pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                git 'https://github.com/ertugrulsmz/IPL-Dashboard'
                bat 'mvn clean compile'

            }
        }
    }
}
